make it so when you upload the file the page refreshes and the new item shows up with a progress bar

Restyle home page with some greebling

Run through the presentation, create 3 min video

Allow editing of files

variance-based video segmentation -- actually somewhat an issue

Create a landing page?

add user accounts + a file length limit + a processing queue, then pop it on steven's server ... or dont


upload to my server
make search work


give it a domain name
letsencrypt?
